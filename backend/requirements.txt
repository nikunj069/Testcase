# Core Framework
fastapi==0.141.1
uvicorn==0.30.1
pydantic==2.13.5
slowapi==0.1.10

# Database
SQLAlchemy==2.0.52

# NLP / NER
gliner==0.2.28
sentence-transformers==6.0.1
fastembed==0.3.4

# Vector Search & Retrieval
faiss-cpu==1.15.0
rank-bm25==0.2.2

# Graph Neural Network
torch==2.13.0
torch-geometric==2.8.0.post1
networkx==3.6.1

# Explainability
shap==0.52.0

# ML Utilities
scikit-learn==1.9.0
joblib==1.6.0
numpy==2.5.2

# Clustering (DOCUMENTED FUTURE WORK — see app/services/nlp/clustering.py)
# These are imported in clustering.py but not called from any live API route.
# Kept because the module is intact and ready for future wiring.
hdbscan==0.8.44
umap-learn==0.5.12

# REMOVED: xgboost==3.4.1
# Reason: Abandoned in favor of scikit-learn HistGradientBoostingClassifier
# due to libomp dynamic library compilation failures on macOS.
# See backend/scripts/benchmark_er_models.py line 78 for the decision rationale.
